{
  "name": "arena-camp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "prebuild": "node scripts/ensure-rollup-native.mjs",
    "build": "npm run build --workspace=arena-camp-frontend",
    "start": "node backend/src/app.js",
    "test": "npm test --workspace=backend"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
